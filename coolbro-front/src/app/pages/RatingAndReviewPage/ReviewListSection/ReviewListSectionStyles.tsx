import styled from 'styled-components'

// Define a styled component for a div
export const ReviewListSectionStyles = styled.div`
  .reviewListCard {
    border-radius: 12px;
    box-shadow: 0px 2px 8px 0px rgba(0, 0, 0, 0.14);
  }
`
